[build]
dockerfilePath = "ainet-voice-agent/Dockerfile"
buildContext = "ainet-voice-agent"

[deploy]
startCommand = "python -m agent.main start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
